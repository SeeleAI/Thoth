# Thoth MVP Goal Prompt

Status: Ready to use
Scope: Goal-mode prompt for long-running MVP implementation
Last updated: 2026-07-06

## Prompt

```text
Advance the Thoth MVP over the long term on the current branch `agent/dev/mvp` in `/mnt/cfs/5vr0p6/yzy/thoth`. Do not continue repairing the current Thoth toy shell, and do not rewrite the frontend from scratch. `NTH-CD-041` has already moved the Loop-2 main path back to the original Paseo production-grade app surface: Paseo is a necessary frontend substrate, and the self-written Thoth toy shell must be deleted, reverted, or isolated as a non-primary path. Integrate only Thoth Clarify / packet / runtime control into the original Paseo session/workspace/task/detail, stream, composer, settings, pane, and responsive layout.

Before starting, read: `AGENTS.md`, `.agent-os/project-index.md`, `.agent-os/todo.md`, the latest entry in `.agent-os/run-log.md`, `.agent-os/designs/the-core-design-principles.md`, `.agent-os/designs/thoth-mvp-user-journey.md`, `.agent-os/designs/thoth-app-runtime-contract.md`, `.agent-os/designs/thoth-mvp-loop-goals.md`, `.agent-os/designs/thoth-engineering-architecture.md`, `NTH-CD-012/013/014/015/016/020/022/027/029/030/031/032/033/034/035/036/037/038/039/040/041/042/045/046/047/048/049/050` in `.agent-os/change-decisions.md`, as well as `packages/app/AGENTS.md` and `.agent-os/upstreams/paseo/packages/app/src`. The protocol code contract is `packages/protocol/src/thoth-runtime-contract.ts`.

Product direction: return the main entry point to the original Paseo app surface and production-grade layout. The following must be retained: stream, timeline, composer, card, settings, host/provider, relay pairing, diagnostics, attachments, file drop, file links, markdown/code/diff/highlighted content, terminal/browser/file panes, workspace/session list/detail, keyboard/focus/accessibility, desktop/mobile responsive layout, and the e2e/test harness. Do not preserve Paseo's product mental model unchanged as a Thoth agent manager, but do not discard Paseo's production-grade UI capabilities in an effort to be “anti-Paseo.” For this round, host background/task/loop functionality in Paseo's original session/workspace/task/detail view system; do not create a separate Background Tasks main view.

Runtime direction: Thoth is neither a hidden LLM API wrapper nor a local natural-language intelligence layer. All AI capability comes from provider sessions. `Quick + none` is the raw-provider / Paseo-like foreground path: it does not load `thoth.clarify`, wrap the input in a Clarify input envelope, require `submit_clarify_packet`, or require structured Clarify output; ordinary text, thinking/progress/tool/evidence stream into the frontend according to the provider's native capabilities. Only `Quick + clarify` loads the hidden `thoth.clarify` and manages behavior with `turn_phase=clarify|approval_task|approval_breakdown|quick_exec|repair`; except for `quick_exec`, every structured phase must submit exactly one qualified packet through `submit_clarify_packet`. The secretary session for `Loop` is responsible only for Clarify and the two confirmation cards; after confirmation, the daemon registers a background task and starts independent `thoth.loop` PlanExec/Review sessions. The `C_ASK` Clarify card, Task Card, and Goal Card must be rendered once as complete cards only after complete provider output and daemon packet/schema/provenance/authority validation have passed. The daemon performs only phase/envelope handling, stream/event projection, schema validation, state transitions, repair, the two confirmation gates, the permission gate, persistence, and client state broadcast.

Proceed in order through the six independently executable loop goals; do not treat the MVP as one large, diffuse task. The current decomposition is centered on the agent harness / prompt engineering / eval harness; the daemon, packet, store, gate, and UI rendering are mechanical safeguards, not the goals themselves: `NTH-TD-015` backend Clarify Agent Harness + Convergence Contract; `NTH-TD-016` frontend Paseo app surface restoration + Clarify runtime control; `NTH-TD-017` backend Task Contract Compiler + Approval Harness; `NTH-TD-018` frontend Task / Goal Approval Experience; `NTH-TD-019` backend Loop Execution + Review Agent Harness; `NTH-TD-020` frontend loop/task dogfood mapping on the Paseo app surface. The current top next action is `NTH-TD-016`; continue with `NTH-TD-017` and the others only after Loop-2 is re-accepted.

Development of `thoth.clarify` and `thoth.loop` must be golden-driven. Do not merely prove that the packet/schema is valid; prepare fixed golden data that records the input, context, expected behavior-tree node, goals/constraints/acceptance, forbidden questions or strategies, and the low user-cognitive-load standard. After every change to a skill, prompt, rubric, or key behavioral constraint, run the golden first, then use an independent session, typically via `codex exec`, to review whether the current transcripts satisfy behavioral psychology, behavior-tree convergence, goals/constraints/acceptance, and low cognitive load. The primary development session cannot self-certify success; if the judge finds irrelevant follow-up questions, a questionnaire-like treatment of fields, discoverable facts pushed to the user, lack of convergence, disregard for the frozen contract, Review reduced to merely running tests, or mechanical repetition on retry, revise the skill/prompt/rubric and rerun.

The highest boundary for Review: treat the Agent Harness as a reviewer with independent judgment, not as a daemon form filler. The daemon stores phase/round/task id, session handle, budget, retry count, envelope, receipt, hash, manifest, event revision, and recovery state, but must never stuff these mechanical fields into the cognitive context, prompt, or tool obligations of Clarify, PlanExec, Review, or audit. Review sees only the user-approved goals/constraints/acceptance, the current goal, real workspace work and inspectable evidence, PlanExec as material that may be challenged, and the semantic Review Direction Memo from the previous round. It must actively challenge the current direction and identify the real crux; when the direction is wrong, it must require abandoning incremental patching and provide the highest-leverage methodological direction for the next round. The independent judge must evaluate this independence, root-cause insight, non-incremental correction, and direction quality; complete schema fields, receipt counts, or consistency with PlanExec's self-report cannot substitute for them.

Latest Clarify policy for Loop 1/2: the behavior tree is a decomposition tree of the user's prompt; each round asks only about the highest-value current branch, preserves the user's original goal, and quickly eliminates wrong directions. No fallback-degrading questions: do not downgrade the user's requested A into an easier MVP, demo, mock, partial implementation, or B. Do not provide a recommendation or default option by default; when a technical judgment is better made by the agent, make and record it yourself, and recommend only when the user asks for a recommendation. `clarify_strength` must affect behavior: `none` means do not enter Clarify and stream directly through the raw provider foreground; `light` asks only about the highest-impact core branch; `balanced` goes 1-2 levels deep into material leaves; `dive` eliminates as many material assumptions as possible without asking about implementation minutiae, common knowledge, a standard answer, or discoverable facts. `thoth.clarify` must distinguish `user_must_decide`, `agent_can_decide`, `agent_can_discover`, and `standard_answer/common_sense`, and ask the user only about high-impact `user_must_decide` items. The behavioral rules for `thoth.clarify` are written in the standard `SKILL.md`; ordinary same-state provider input carries only phase/state/controls, transcript ref, assumption ledger ref, and decision-tree frontier ref. It does not repeat the Skill rules or carry `skill_ref`. Carry `skill_ref` / digest marker only at session start, state transitions, skill digest/version changes, context loss, or repeated repair failures; an intensity change may carry `controls_changed`. `hi` under Quick+none must be a raw provider stream with no packet and no card; under active Clarify it may be `C_DIRECT`, but it must remain brief and natural, with no daemon-local fixed greeting.

Clarify question/answer cards may refer to the rendering capabilities of Paseo's existing Codex request-user-input card, but the product semantics remain those of the hidden `thoth.clarify`. Each question card has a title, one primary behavior-tree node, and 2-4 closely related questions; each question has short branch choices, with each choice label no longer than 10 Chinese characters and its explanation no longer than 20 Chinese characters. The Answer packet supports adding a note to the selected choice for each question, and also supports selecting no option and writing only a note. `C_ASK` may carry hidden `content.meta` recording effective strength, tree depth, QA round count, remaining material assumptions, and the reason for the question's value, but these are not shown directly to the user.

The final two confirmation rounds of `thoth.clarify` must preserve provenance. The `content` of the `C_TASK_CARD` packet must carry the verbatim transcript of all preceding Clarify questions and answers; the `content` of the `C_GOAL_CARD` packet must carry both that transcript and the verbatim original of the overall CEO Task Card confirmed by the user in the first round. Goal decomposition must be grounded in the transcript + approved CEO card; it may not rely only on the provider's implicit memory, and it must not invent, lose, alter, or over-interpret goals, constraints, acceptance, or risks.

MVP path to implement first: the original Paseo composer accepts the user's prompt together with Provider / Clarify / Mode controls and workspace/session/topic context. When Mode=Quick and Clarify=none, use the raw provider stream and do not enter the Clarify packet flow. When Mode=Quick and Clarify is not none, the daemon constructs a phase-aware provider input envelope, and the provider session runs through the `thoth.clarify` runtime skill and `submit_clarify_packet` bridge; atomically insert the Clarify card only after complete provider output and daemon schema/packet validation of `C_ASK`; likewise atomically render `C_TASK_CARD` / `C_GOAL_CARD` only after complete validation; after user confirmation, enter `quick_exec` in the same session and execute through the ordinary stream. In Mode=Loop, issue `C_REGISTER` only after confirmation, and execute in the background through independent PlanExec/Review sessions. An invalid packet enters repair/blocker; high-risk actions must pass through a permission card.

Special Loop-2 frontend constraints: this is a hard APP frontend refactoring gate, but the direction is not to continue the three-view toy Workspace Secretary; it is to restore/retain the original Paseo frontend app capabilities and layout. A toy shell such as `packages/app/src/thoth-app/thoth-app-shell.tsx` cannot be the main entry point; delete it where possible, and if short-term type dependencies exist, isolate it as a legacy/deprecated non-primary path. Remove toy/debug/acceptance/contract language from the main interface: `WORKSPACE SECRETARY`, `current requirement convergence`, `Quick foreground · Loop background`, `real provider connected`, `Quick and Loop both write real provider results to history`, `current secretary topic`, `new secretary topic`, full local paths, `provider-backed clean UI model`, `C_DIRECT` / `C_ASK` / packet / repair / schema / raw JSON / provider role, and so on. The Clarify card must be a Thoth decision card supporting 2-4 closely related questions, short choices, short explanations, per-option note, note-only, “you recommend,” and “you decide,” with no default preselection or default recommendation, and it must render stably in the Paseo transcript / agent-stream. The UI consumes only typed clean UI model / clean streaming event; it must not parse assistant text, markdown JSON, code fences, snippets, or raw packets, and must not locally judge convergence, generate Task/Goal Cards, modify authority, or choose on the user's behalf.

Constraints: do not restore archived Python/plugin/Textual/voice/audio; do not make hidden calls to a general-purpose LLM API; do not use local rules to fake semantic intelligence; do not touch Paseo `6767`; retain the Thoth daemon `6688`, Web `8082 -> 8148`, and relay test `relay.test.thoth.seeles.ai`, and use the real Seele relay service for Loop-2 acceptance; do not pass off `#`, `example.com`, a localhost relay, mock success, a fake device link, or an offline fixture as a real relay. `No fallback` means no functional fallback: without a real provider, real relay, qualified packet, or verifiable bridge, show only honest unavailable / blocked / needs provider / unsupported / needs relay and block the action; do not pretend completion with a local fixed reply, first-option fallback, provider waterfall, assistant markdown JSON extraction, text parsing, fake provider, fake card, or fake relay. The relay pairing token must not be written into a URL query, logs, screenshots, docs examples, or the final report. The UI may reuse existing components and icons, but the current scorecard shell is a rejected legacy baseline, not the target IA.

Acceptance: actually run the anti-toy-shell / anti-Paseo-product-semantics residual scan, Paseo capability retention scan/source review, authority-boundary source review, stream/render source review, Clarify card component/unit tests, `npm --workspace=@thoth/app run test`, Loop-2 narrow real-provider e2e, `npm run build:web`, acceptance against the real `relay.test.thoth.seeles.ai`, and `npm run check:foundation`. Save desktop/mobile screenshots, screenshots showing retained original Paseo layout, Provider/Clarify/Mode composer screenshots, a Quick+none raw provider stream screenshot or trace, an active Clarify `C_DIRECT` or `C_ASK` atomic card, Task/Goal approval, submitted readonly, same-session quick_exec, the real relay status in Settings, and the Playwright trace/video, and actually open and review them with `view_image`. Finally, use an independent `codex exec` for a UI mental-model review; if it finds a toy shell, broken Paseo capability, degraded composer, unstable Clarify card, Quick+none made protocolized, a non-atomic QA card, the app parsing raw packet/markdown JSON, fake provider/relay/mock success, user-visible debug/acceptance copy, or a separate Background Tasks toy view, mark FAIL. After completion, update `.agent-os/run-log.md`, `.agent-os/acceptance-report.md`, `.agent-os/project-index.md`, `.agent-os/todo.md`, and `.agent-os/architecture-milestones.md`, and update lessons if necessary; without complete evidence, do not mark verified.
```

## Loop-2 Goal-Mode Prompt

```text
You are the frontend lead for Thoth Loop-2: a top-tier product engineer, React Native/Web architect, and rigorous UX reviewer. The repository is `/mnt/cfs/5vr0p6/yzy/thoth`, on branch `agent/dev/mvp`. First recover context according to `AGENTS.md`; do not downgrade the task into a small UI patch. This round should embrace refactoring risk and head directly toward the final product goal.

Authority: for Loop-2, consult `NTH-TD-016` in `.agent-os/designs/thoth-mvp-loop-goals.md`; for decision boundaries, consult `NTH-CD-036/037/038/039/040/041/042` in `.agent-os/change-decisions.md`, where `NTH-CD-041` covers the three-view toy-shell main path and `NTH-CD-042` covers the Quick+none / Quick+clarify / Loop runtime phases; for the condensed milestone version, consult `NTH-MS-013` in `.agent-os/architecture-milestones.md`.

Hard goal: return the original Paseo production-grade app surface and layout as the main entry point. Paseo is a necessary frontend substrate, not a temporary reference; retain stream/timeline/composer/card/settings/host-provider/attachments/file links/terminal/browser/file panes/desktop-mobile responsive layout/e2e capabilities. The current Thoth toy shell cannot be the main entry point; remove toy/debug/acceptance/contract copy; do not create a separate Background Tasks main view; Thoth should integrate only Clarify runtime, input packet, output packet, composer controls, and the authority adapter.

Hard constraints: the UI may consume only typed clean UI model / clean streaming event; it must not parse assistant text, markdown JSON, code fences, snippets, or raw packets. `Quick + none` must remain a raw provider stream and must not be wrapped as a Clarify packet/card. `C_DIRECT`, provider progress, tool/progress/evidence in active Clarify should be streamed wherever possible. `C_ASK` / Task Card / Goal Card must be rendered atomically only after complete `submit_clarify_packet` + daemon validation. Do not locally judge convergence, generate Task/Goal Cards, modify authority, or choose for the user; do not use default preselection, default recommendations, or fallback degradation; do not use voice/audio/dictation; do not touch Paseo `6767`; do not pass acceptance with fake relay links, a localhost relay, mock success, a fake device link, or an offline fixture.

You must implement the real user journey: Quick+none `hi` is a raw provider stream, produces no card, produces no packet, and does not enter repair; Quick+clarify supports Clarify -> Task Card -> Goal Card -> same-session quick_exec; Quick->Loop->Clarify->two-card confirmation->background registration->Quick remains stable; the Clarify card supports 2-4 closely related questions, short choices, short explanations, per-option note, note-only, “you recommend,” and “you decide”; submitted cards are readonly; multiple rounds do not overwrite history; both mobile and desktop work; Settings must show the secure status of the real Seele relay test service `relay.test.thoth.seeles.ai` without leaking the token/raw offer/credential.

Final acceptance: along the real user journey, actually run the anti-toy-shell / anti-internal-copy scan, Paseo capability retention source review, authority-boundary source review, stream/render source review, Clarify card unit/component/e2e, `npm --workspace=@thoth/app run test`, Loop-2 narrow e2e, `npm run build:web`, acceptance against the real `relay.test.thoth.seeles.ai`, and `npm run check:foundation`. Produce desktop/mobile screenshots, screenshots showing retained original Paseo layout, Provider/Clarify/Mode composer screenshots, a Quick+none raw provider stream screenshot or trace, an active Clarify `C_DIRECT` or `C_ASK` atomic card, Task/Goal approval, submitted readonly, same-session quick_exec, the real relay status in Settings, and the Playwright trace/video, and review them with `view_image`. Finally, use an independent `codex exec` for a UI mental-model review; if it finds a toy shell, broken Paseo capability, degraded composer, unstable Clarify card, Quick+none made protocolized, a non-atomic QA card, reliance on fallbacks/old entry points/text parsing/fake provider/fake relay, user-visible debug copy, or a separate Background Tasks toy view, mark FAIL. After completion, update the `.agent-os` evidence ledger.
```
