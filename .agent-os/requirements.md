# Requirements

## Objective

`NTH-OBJ-001`: Thoth must become a host-neutral task control plane for AI work. It should behave like a reliable private secretary for a high-authority user: remember context, understand vague intent, ask only the important golden questions, register durable tasks when needed, run asynchronous loops, and report results with evidence.

## Goals

1. `NTH-REQ-001`: Reduce user cognitive burden and entry barrier as the first design principle.
2. `NTH-REQ-002`: Compile natural-language intent into clear task contracts with goal, non-goals, constraints, assumptions, risks and acceptance.
3. `NTH-REQ-003`: Treat `Loop` tasks as recoverable, reviewable, long-running loops rather than one-off agent runs.
4. `NTH-REQ-004`: Make success depend on frozen acceptance and evidence, not executor self-report.
5. `NTH-REQ-005`: Keep Thoth host-neutral across Claude Code, Codex, ACP-compatible tools and future harnesses.
6. `NTH-REQ-006`: Keep UI shells thin. TUI, desktop app, mobile app and CLI must see the same authority.
7. `NTH-REQ-007`: Use OpenTUI for the TUI shell.
8. `NTH-REQ-008`: Use a TypeScript / Node monorepo for the new runtime. The new core must not use Python as the main product runtime.
9. `NTH-REQ-009`: Preserve archived plugin history through archive release and archive branch, not through legacy code in the active working tree.
10. `NTH-REQ-010`: Provide an explicit GitHub Actions release and packaging pipeline. Under `NTH-CD-056`, the current MVP trigger is a push to the dedicated `release/mvp-actions` branch and replaces only the fixed `v0.0.0-mvp-beta` prerelease after every build passes. It produces desktop installers, a signed Android APK and a GitHub-hosted server CLI tgz without npm publication or iOS self-install claims; production web/relay deploy and iOS distribution remain separate decisions.
11. `NTH-REQ-011`: Keep Thoth as a control plane, not a harness or direct LLM client. All AI execution must occur through configured provider sessions via ACP, harness runtime, app-server, official harness SDK/control surface or local harness CLI.
12. `NTH-REQ-012`: Keep semantic routing provider-backed or user-selected. The product exposes explicit controls for `Quick` versus `Loop`, clarification strength and loop strength, but Thoth core/daemon must not classify natural-language intent with local heuristic rules.
13. `NTH-REQ-013`: Use the locked chatbox composer controls: `+`, Provider, Mode, Clarify and Loop. Provider owns model/runtime/permission/fast settings; Clarify has five levels: `auto`, `Don't Bother Me`, `light`, `Balanced`, `deep`; Loop has five levels: `auto`, `One Plan, One Do`, `light`, `balanced`, `Run Until Stopped`; `Run Until Stopped` must be visually high-risk, high-cost and manually stopped.
14. `NTH-REQ-014`: Use provider-native sessions for the business flow. `Quick + Don't Bother Me` is a provider passthrough path. `Loop` uses read-only Clarify, frozen contract, one PlanExec provider session with provider-native plan mode when available, and independent Review. Visible provider output must stream to clients in real time. PlanExec clarification questions after contract freeze are auto-answered from the frozen contract or first recommended option, while provider permission requests still obey permission policy.
15. `NTH-REQ-015`: Use `AGPL-3.0-or-later` for Thoth and allow upstream-derived implementation seed material only when provenance, license, commit SHA, exclusion policy and expected broken state are recorded. Multica source code must not be copied into this repository.
16. `NTH-REQ-016`: Maintain first-day development infrastructure before feature work: stable npm install, root-script validation gates, foundation build/typecheck/test coverage, local Android Debug APK packaging, Linux-safe iOS script behavior, package-level agent contracts and executable development/testing/packaging/release docs.
17. `NTH-REQ-017`: Provide a stable human dogfood entry for Thoth I whose UI and experience match the current releasable full UI. Development mode may change runtime wiring, logs, local daemon targets or provider configuration, but it must not change the user-facing flow, layout, copy, states or task experience into a separate debug product.
18. `NTH-REQ-018`: Keep Thoth runtime independent from the local Paseo daemon. Thoth direct daemon defaults to `127.0.0.1:6688`; local Paseo/legacy `127.0.0.1:6767` must remain untouched; web review uses the real app on `8082 -> 8148`; relay test traffic uses `relay.test.thoth.seeles.ai` through v3 tokenized pairing; desktop and Android artifacts must carry Thoth identity and avoid legacy/Paseo runtime state.
19. `NTH-REQ-019`: Make `Simply Is First` the highest code-development priority. Development must implement the locked final architecture as independently verifiable production modules, one real module at a time. Rapid experiments must exercise the final module boundary and product path; they may reveal failing metrics, but may not introduce simplified substitutes, parallel implementations, fallback behavior, semantic downgrades or unapproved architecture changes.
20. `NTH-REQ-020`: Persist and orchestrate Thoth through provider-neutral Workspace and Task authority. One Workspace owns one authority shard and one mutation lease domain; every approved Quick or Loop execution is a Task; every provider run is a fenced ExecutionAttempt; every human authority decision is append-only and exact. RuntimeBundles are deduplicated by digest and attached through the same HarnessAdapter contract for Codex, Claude Code, OpenCode, Pi and ACP. Provider homes, authentication, native transcripts and caches remain provider-owned and must never be copied into `.thoth`.
21. `NTH-REQ-021`: Treat native Plan, Plan-to-Implement transition and provider approvals as one capability-based Harness contract for foreground and background execution. Loop PlanExec requires a native Plan receipt; unsupported providers cannot register Loop through a hidden prompt fallback. Background runtime approvals are durable, task-scoped, CAS-fenced and auto-approved once after a fixed 20-second human window, while Thoth authority Cards remain human-owned. Installed App recovery must reconcile persisted Agent authority before restoring tabs or emitting live state.
22. `NTH-REQ-022`: Run deletion-only refactor slices without changing valid product behavior. Remove retired and unreachable code at its final package boundary, keep every slice net-negative in code and complexity, preserve the sole production authority path, and reject replacement stubs, compatibility shells, hidden fallbacks or temporary parallel implementations. Verify each slice through an affected source-level gate that completes within five minutes.
23. `NTH-REQ-023`: Keep foreground Send, Queue, Interrupt, canonical Timeline and rewind identity under one Workspace authority. Queue must survive restart/reconnect and preserve the full frozen turn snapshot; Interrupt must never overlap provider turns. Provider-native rewind anchors are opaque Harness receipts, not UI ids or provider-specific daemon logic. App file previews must render daemon/Relay image bytes transiently without becoming durable attachments.
24. `NTH-REQ-024`: Make native Plan an Agent-scoped provider control backed by the live Harness session. Each Agent independently persists `default | plan`; capability probing must distinguish native support, honest unsupported and temporary unavailability; the App presents Plan as a row inside `Provider Features`, never as a separate Run Mode section or Composer control; provider Plan results remain in the canonical Timeline after Implement/Reject and restart. Closing a top-level Agent tab must durably archive it before removing local layout. The fixed MVP GitHub Release is desktop-only: macOS, Windows and Linux packages plus metadata; Android and server CLI are not public Release assets, although server CLI remains an internal Relay/daemon acceptance artifact and mobile source/local builds remain available.
25. `NTH-REQ-025`: Converge Thoth onto one final production main chain without changing business flow, algorithms, public semantic capability or UX, and reduce git-tracked production TypeScript/JavaScript by at least `50,000` physical lines from the clean `743e8d29` baseline. Every atomic cut must leave production LOC, scanner tokens, AST nodes and non-type static import edges lower, add no runtime dependency, preserve the complete public Client/Protocol/Provider/AgentTimeline/VCS surface and measurably improve the module it owns. Final performance must improve daemon cold-ready by at least `40%`, daemon idle RSS by at least `30%`, first interactive App Workspace by at least `25%` and local response overhead median by at least `30%` without p95 or idle-CPU regression.
26. `NTH-REQ-026`: Complete the feature-zero-loss `50,000` production-LOC reduction before performance optimization. The remaining cutovers use compositional OOP at stateful and replaceable boundaries, preserve every public capability, AgentTimeline field and UX interaction, and remove each replaced implementation in the same atomic commit without runtime fallback or dual path. The performance targets in `NTH-REQ-025` are deferred to `NTH-MS-019`; they are not completion conditions for `NTH-MS-018` and are not weakened or rewritten.
27. `NTH-REQ-027`: Persisted App layout may remember presentation intent but must never fabricate current Agent or Terminal authority. A restored entity tab becomes visible and actionable only when the current daemon/query authority identifies that entity; archived, deleted, migrated-away or cross-device-removed entities are pruned without issuing a second destructive command. Closing a known active top-level Agent still archives before local removal, while missing/archived Agents and missing Terminals are local reconciliation cases rather than RPC fallbacks.
28. `NTH-REQ-028`: Organically integrate the useful capabilities and bug fixes in the exact Paseo range `5fc53c576ef0d4dee55455ccc95660703f71b892..b589599a8f21bcc9e4c911603082566ce320a3c8` through Thoth's canonical Workspace/Task/HarnessAdapter architecture. Every one of the 393 upstream commits must have explicit provenance; all 143 architecture candidates and 43 required reviews must be resolved before product-source edits. Accepted Forge, Browser, Schedule, Timeline, Provider, Files/Changes, Desktop/Terminal and App/Mobile behavior must land on one final Protocol/Client/Daemon/Core/Drivers/shell path. Voice/audio, Hub/cloud truth, website, legacy AgentManager or file-JSON authority, runtime `127.0.0.1:6767` fallback, direct file editing and unapproved distribution paths remain rejected or deferred as decided by `NTH-CD-076` through `NTH-CD-088`.
29. `NTH-REQ-029`: Organically integrate the useful capabilities and bug fixes in Paseo v0.2.3's exact target-side set `v0.2.2...v0.2.3 --right-only`, equivalently `36f38245cab51bbe0b43b6ac42fd41aa757064d9..43cf858c3760679ec9be805ba8b903cdf20f7103`, while preserving Thoth's increasingly divergent Workspace/Task/HarnessAdapter architecture. The 36 commits, 211 changed paths, 12 architecture candidates and four required reviews must have complete schema-version-2 provenance before product-source edits. Accepted Tasks/Schedules, Workspace-script, Provider, transport, service-proxy, Desktop-lifecycle and App/Mobile behavior must replace the affected path through current Protocol/Client/Daemon/Drivers/shell ownership; voice/audio, Hub/cloud truth, Paseo Relay endpoints, Nix/store/release topology, AgentManager, file-backed JSON authority and runtime `127.0.0.1:6767` remain rejected.
30. `NTH-REQ-030`: Preserve exactly one native Provider session for each visible Agent while making Thoth RuntimeBundle activation, Provider-native Plan, Provider-native questions and Implement approval independent per turn. A Thoth-enabled turn may invoke the canonical runtime Skill and leave normal conversation history, but a later raw turn must not receive the RuntimeBundle again. Provider questions must retain native ids and structured answers, only a completed native Plan item may authorize Implement, and neither assistant prose nor progress/delta events may be promoted into a Plan. Thoth tools remain session capabilities fenced by the current Workspace/Agent/turn/generation binding; no replacement session, provider-name business branch, prompt-simulated Plan, first-option answer fallback or second authority path is allowed.
31. `NTH-REQ-031`: Publish the verified same-session RuntimeBundle, native Plan and Provider-question correction through the existing fixed desktop-only Beta transaction. One exact release-source SHA must be normally fast-forwarded to `agent/dev/mvp` and `release/mvp-actions`; every mandatory native, packaged, Server CLI, hosted Relay and publish job must pass before the old fixed Release is replaced. Publication is complete only after public metadata, exactly 26 desktop-only assets, checksums, update manifest, embedded build identity and a freshly downloaded AppImage real-window Provider Plan/question journey all agree with that SHA.
32. `NTH-REQ-032`: Replace quota/form-driven Clarify and linear-Goal Loop with one Provider-neutral cognitive-control architecture. Clarify must maintain a durable user-visible Decision Map, autonomously resolve Agent/Evidence-owned branches, ask only material Human-owned branches without a global question cap, and freeze one human-confirmed Intent Contract. Every Task must keep one immutable Task Anchor while Provider Agents autonomously manage a mutable Working Set and Work Units; every meaningful execution checkpoint must enter fresh independent Review, and context pressure, semantic drift or repeated no-progress must trigger an explicit context reset. Current schema-v5 installations must migrate losslessly to this authority without retaining Task/Goals runtime compatibility, and the verified result must replace only the fixed desktop Beta Release.
33. `NTH-REQ-033`: Make Clarify a durable sequence of Decision Sessions inside one visible Agent conversation. Each Decision Session owns exactly one deterministic decision tree, one Intent Contract and at most one Task; it may span arbitrarily many provider turns, but Task registration is forbidden until every material Human frontier is resolved or delegated, every Agent/Evidence frontier is grounded, the single Challenger has completed and the final contract is confirmed. The App must render this authority as an incremental, responsive left-to-right tree mind map with stable identity, explicit activity, one active Card inspector and frozen Task evidence. Thoth-off turns must neither create nor advance a Decision Session. Current schema-v6 installations must migrate atomically to schema v7 without a legacy runtime path, and the verified result must replace only the fixed 26-asset desktop Beta Release.
34. `NTH-REQ-034`: Keep every durable visible Agent usable even when its Provider-native thread cannot currently resume. Provider capability refresh for such an Agent must preserve Timeline authority and return a typed provider-neutral unavailable projection instead of throwing a raw missing-Agent error or inventing a live session. Decision Tree presentation must derive from the actual Agent-pane container, keep readable conversation space, place every node and frozen Task leaf inside one complete deterministic scene, route connectors without intersecting decision nodes, and remain responsive without content overflow or forced viewport jumps. The verified correction must replace only the fixed 26-asset desktop Beta Release.

## Acceptance Criteria

1. `NTH-AC-001`: The active working tree no longer contains archived Python runtime, plugin projection, dashboard template, Textual TUI or old tests.
2. `NTH-AC-002`: The active working tree contains exactly the 10 approved package skeletons under `packages/`.
3. `NTH-AC-003`: Root and package metadata use `AGPL-3.0-or-later`, the current decision-locked package version `0.0.0-mvp-beta`, and `npm workspaces`.
4. `NTH-AC-004`: The recovery path from `AGENTS.md` to `.agent-os/project-index.md` to `.agent-os/todo.md` can explain the current Thoth state without the chat transcript.
5. `NTH-AC-005`: The canonical design set is present under `.agent-os/designs/`.
6. `NTH-AC-006`: The archived plugin release and branch are documented for traceability.
7. `NTH-AC-007`: Claims about runnable Thoth surfaces are backed by current build, test, packaging or release evidence and clearly distinguish local development, MVP beta and archived plugin paths.
8. `NTH-AC-008`: `npm run check:foundation` passes through repo validation, formatting, foundation lint, foundation build, foundation typecheck and foundation tests.
9. `NTH-AC-009`: `npm run package:android:debug-apk` produces a real local Debug APK and records its absolute path, sha256 and byte size without committing the APK or generated native project.
10. `NTH-AC-010`: Root plus all 10 packages have local `AGENTS.md` files, and every `CLAUDE.md` is a symlink to the matching `AGENTS.md`.
11. `NTH-AC-011`: Once the Thoth I dev entry exists, human review enters the same UI component tree, routes, composer controls, task cards, stream states and report surfaces that a releasable build uses; debug overlays or logs may exist only as non-primary developer aids.
12. `NTH-AC-012`: Runtime isolation smoke proves Paseo/legacy remains on `127.0.0.1:6767`, Thoth runs on `127.0.0.1:6688`, the real web UI responds on `8082 -> 8148`, the test relay health endpoint responds, Linux AppImage packaged smoke passes with an isolated managed daemon, Android Debug APK has package id `sh.thoth.debug` without `RECORD_AUDIO`, and Codex provider smoke passes through Thoth paths.
13. `NTH-AC-013`: The authorized `v0.0.0-mvp-beta` GitHub Actions flow leaves `main` unchanged, publishes exactly one current MVP prerelease with native macOS/Windows/Linux, signed Android and server CLI assets, preserves the historical archive release, does not publish npm packages and passes independent downloaded-asset verification.
14. `NTH-AC-014`: Every nontrivial implementation slice identifies its canonical final module, production interface, state owner and real-path acceptance before coding; its landed code remains part of the final architecture, introduces no unapproved parallel or fallback path, and reports unmet acceptance values as failures rather than lowering the target.
15. `NTH-AC-015`: A fresh packaged AppImage proves raw/Thoth hot switching on one visible provider thread, durable Card continuation after a five-hour wait, Quick and Loop Tasks, `thoth.loop` attachment receipts for every background phase, failed-Review retry, same-Workspace structured `@Task` context, Stop without a stale phase spinner, daemon restart recovery and a provider-session-free `.thoth` layout. Six scripted sessions add at most 25MB of durable Thoth state and store each RuntimeBundle digest only once. The same public adapter conformance journey passes for Codex, Claude Code, OpenCode, Pi and ACP; Codex additionally passes a real-provider run.
16. `NTH-AC-016`: One fast public-API gate proves native Plan selection for every supporting adapter, honest unsupported behavior for Pi/non-Plan ACP, foreground mode snapshots independent of Thoth, Loop PlanExec Plan-to-Implement continuation, 20-second background approval CAS/timeout/Stop fencing, and update-time Agent/tab reconciliation. The gate uses real product adapters and authority with deterministic external transports and a fake clock, completes within five minutes, and contains no AppImage, Relay, browser or real-provider smoke.
17. `NTH-AC-017`: A deletion-only slice is acceptable only when its surviving public behavior tests pass, retired symbols and file references are absent, relevant type boundaries remain green, its conservative non-overlapping diff is net-negative, formatting/diff hygiene passes for the slice, and the complete acceptance bundle finishes within 300 seconds without packaging, Relay, browser or real-provider work.
18. `NTH-AC-018`: One source-level gate under 300 seconds proves Queue/Interrupt ordering, Card suspension, restart recovery, CAS/idempotency, one canonical user Timeline row, provider-neutral opaque rewind anchors and epoch reset, Queue-default settings migration, daemon/client binary image reads, transient PNG/JPEG/GIF/WebP preview creation/release and removal of App-local Queue/optimistic Timeline/preview-copy paths. Release promotion remains delegated to the existing native Actions workflow.
19. `NTH-AC-019`: One bounded gate proves live-session Plan capability, Agent-isolated run-mode persistence, Default -> Plan -> Default on one provider thread, durable non-duplicated Plan results, a Plan feature row inside Provider settings with no separate Run Mode or Composer control, and archive-before-layout behavior for single/bulk tab close and restart. A real Codex public Create/Send journey must produce a native Plan receipt, visible Plan result and same-thread Implement continuation. The replacement Release must contain only macOS, Windows, Linux and metadata assets; it must contain no APK, iOS package or server CLI tgz.
20. `NTH-AC-020`: `npm run accept:refactor:fast` is the sole refactor acceptance entry and shares one hard `300s` deadline across architecture/source/storage guards, foundation, affected behavior, real Web visual/keyboard/focus/a11y/responsive transcripts, TUI frame and seven-sample performance checks. Every cut is an atomic single-path replacement with no old/new fallback, preserves the Release `05775486` SQLite fixture by entity digest and failure-safe migration, and passes the frozen public capability manifest. Final acceptance additionally proves at least `50,000` production LOC removed, all four performance targets, source daemon/Client Relay pairing-E2EE-reconnect/Quick/Card/Loop/Timeline and zero interaction with reserved Paseo `127.0.0.1:6767`; it excludes native packaging, real Provider, push, tag, Release and cloud publication.
21. `NTH-AC-021`: During `NTH-MS-018`, `npm run accept:refactor:fast` retains one shared `300s` deadline for architecture/source/storage guards, foundation, complete public behavior, canonical AgentTimeline replay, Release `05775486` migration, real Web visual/keyboard/focus/a11y/responsive transcripts, TUI and final source Relay coverage. It excludes the three performance phases while preserving their scripts and baselines for `NTH-MS-019`. Final acceptance requires production LOC at or below `258,531`, lower scanner tokens/AST/static imports, no added runtime dependency, complete Feature Lock coverage and no old/new runtime dual path; it does not permit feature, data or UX reduction.
22. `NTH-AC-022`: Promotion of the current refactor intermediate state requires one monotonic local `3600s` deadline covering the complete refactor gate, full App/Daemon/Desktop/CLI and public product suites, Release contracts, golden judges, isolation, freshly built AppImage and hosted Relay journeys, seven-sample daemon/response and App statistical no-regression checks, a 200-client ten-minute local Relay load test and OpenTUI PTY stress. Any failure or timeout stops commit, push and Release mutation. GitHub Actions and public-asset verification run afterward and must pass independently; the fixed Release is replaced only by the workflow after every build and smoke succeeds.
23. `NTH-AC-023`: Agent/Terminal tab reconciliation acceptance seeds legacy persisted layouts containing active, archived, deleted/unknown and stale Terminal targets, then proves authority-backed tabs remain stable while stale targets never render as actionable tabs, never enter single or bulk destructive RPCs and are removed from persisted layout after hydration. Closing a known active root Agent still archives once; known subagent, archived Agent and missing Agent close layout-only; genuine transport/archive errors for known active Agents remain visible. The focused source gate, complete App suite, real Web build, interaction regression and shared five-minute refactor gate must pass without changing Protocol or daemon archive semantics.
24. `NTH-AC-024`: Publishing the persisted entity-tab repair requires one source commit on both authorized branches, an exact-source-SHA GitHub Actions run with every Server CLI, Linux/macOS/Windows smoke, native Desktop, packaged Linux journey, hosted Relay and publish job green, and an in-place `v0.0.0-mvp-beta` replacement whose tag, Release target, `BUILD-SOURCE.txt` and `MVP-UPDATE.json` identify that source. The public Release must contain exactly the existing 26 desktop-only assets; downloaded checksums and AppImage identity must match, and the checksum-verified downloaded AppImage must pass the complete public packaged journey before the publication is verified.
25. `NTH-AC-025`: Paseo v0.2.2 integration is acceptable only when schema-version-2 provenance covers all `393/393` commits, explicitly assesses all `143/143` architecture candidates, classifies every one of the `43/43` required candidates as architectural, has zero pending reviews and zero failures, and accepted behavior uses only current Thoth owners and formal interfaces. Focused behavior, package type/build, boundary, Foundation, Provider Control, interaction, complete Thoth, refactor, real Web, local Android Debug APK and local AppImage journeys must pass in the current run. Publication then requires one exact release-source SHA normally fast-forwarded to `agent/dev/mvp` and `release/mvp-actions`, every mandatory native workflow job green before Release mutation, an in-place `v0.0.0-mvp-beta` replacement with exactly 26 desktop-only assets, downloaded checksum/build-identity agreement and a successful public AppImage journey, while `main`, Relay deployment and the independent Paseo service remain untouched.
26. `NTH-AC-026`: Paseo v0.2.3 integration is acceptable only when provenance covers all `36/36` target-side commits, assesses all `12/12` architecture candidates, classifies every `4/4` required candidate as architectural, and reports zero pending reviews and zero failures. The App must expose one `Tasks` surface with `Tasks | Schedules`, full Schedule CRUD/control/history and Schedule-Task-Execution navigation; Workspace scripts must use one scoped Client/ToolGateway/runtime-lease path; Provider-specific usage and OMP capability parsing must reside in Drivers; socket liveness, binary E2EE and bounded large-file streaming must land atomically; and service-proxy authority must reject untrusted forwarded headers. Focused tests, complete package suites, boundary/provenance, Foundation, Provider Control, interaction, complete Thoth, refactor, real Web, direct and hosted Relay transport, local Android Debug APK, isolation and local AppImage journeys must pass in the current run. Publication requires one exact source SHA normally fast-forwarded to both authorized branches, every mandatory workflow job green, an in-place fixed Beta with exactly 26 desktop-only assets, public checksum/build-identity agreement and a successful downloaded-AppImage product journey; `main`, Relay deployment, mobile/store/npm/Nix/Docker publication and independent Paseo remain untouched.
27. `NTH-AC-027`: One visible Agent must retain the same native Provider session through `raw -> Thoth Clarify -> Thoth off -> native Plan -> provider question -> completed Plan -> Implement`. Raw turns contain no new RuntimeBundle Skill activation or generated Clarify developer instructions; every Thoth turn contains exactly one digest-verified per-turn activation; remembered Thoth tools are inert outside an authorized ToolGateway binding. Provider questions render through a dedicated semantic Client API, preserve question ids and answer arrays, reject invalid/stale responses without selecting defaults, and never persist secret answers. Pending questions block Plan completion; ordinary assistant prose, Plan progress and deltas never open Implement; exactly one bounded completed native Plan item produces the Daemon-owned Implement approval. Deterministic owner tests, the complete App suite at or above `363 files / 2,709 tests`, Foundation, Provider Control, interaction, complete Thoth, refactor, real Codex, real Web, local AppImage and isolation checks must pass before verification.
28. `NTH-AC-028`: The Provider-interaction correction is published only when both authorized branches normally fast-forward to one audited release-source SHA, the triggered `MVP Beta Release` workflow has that exact `headSha`, all mandatory jobs succeed, and fixed prerelease `v0.0.0-mvp-beta` is non-draft with exactly 26 desktop-only assets targeting that SHA. Fresh public downloads of `BUILD-SOURCE.txt`, `MVP-UPDATE.json`, `SHA256SUMS` and `Thoth-x86_64.AppImage` must agree on source, workflow, size, SHA-256 and embedded build identity. The downloaded AppImage must pass the complete existing product journey plus `Provider Features -> QuestionFormCard -> completed PlanCard -> Implement` with question-id array answers and same-thread implementation. `main`, Relay deployment, npm/mobile/store/Nix/Docker publication and independent Paseo remain untouched.
29. `NTH-AC-029`: One public-product acceptance proves a same-session Clarify Decision Map with 1-4 related questions per Card, recommendation and subtree delegation, a Dive scenario with more than 30 material Human-owned decisions, exactly one final independent Challenger and one Intent Contract approval. Quick executes once on the visible thread; Loop has no linear Goal authority, uses Task Anchor plus token-budgeted Working Set, checkpoints every meaningful reality delta, performs fresh independent Review, resets rotten context, preserves rejected routes, respects Single/Light/Balanced/Infinite budgets and reopens Clarify for new Human-owned decisions. Schema-v5 Task/Card/Decision/Timeline/Schedule data migrates atomically to schema v6 with no old runtime path. The fast gate completes within 300 seconds, all adapter conformance and desktop packaging gates pass, and the replacement fixed Beta is publicly reverified from downloaded assets.
30. `NTH-AC-030`: One formal product journey proves that a visible Agent can create multiple sequential Decision Sessions, while every Decision Session spans multiple Clarify rounds, owns one stable root and registers no more than one Quick or Loop Task. Revision-fenced snapshots and ordered deltas keep a 500-node deterministic tree responsive across refresh, reconnect and daemon restart; Card submission atomically changes the same stable Card into a read-only receipt; activity remains visibly running until real terminal state; and no stale Card error, premature `Worked for` footer, full-map polling or provider-specific branch exists. Schema-v6 fixtures migrate atomically to schema v7 with deterministic tree parents and preserved cross-links. The shared fast gate finishes within 300 seconds, native/packaged/Relay gates pass, and the fixed desktop Beta is verified again from public downloads.
31. `NTH-AC-031`: A stored visible Agent whose Provider thread cannot resume retains its canonical Timeline, and Provider Features refresh returns `unavailable` with an actionable reason and no raw `Unknown agent`; a later successful resume can replace that history-only projection and run the real capability probe. Decision Tree geometry tests prove fixed node content does not overflow, small trees fit from a stable top-left inset, the frozen Task leaf is fully inside scene bounds, and its routed connector intersects no decision node. Real Web screenshots at wide and constrained Agent-pane widths prove the tree never covers the conversation, narrow panes use the full-screen surface, folded summaries remain contained, and the existing 500-node virtualization path remains responsive. Source gates must pass before one exact SHA is normally pushed to both authorized branches; the fixed Release is replaced only after every mandatory workflow job succeeds and public metadata/desktop assets are reverified.

## Hard Constraints

1. Do not push unless explicitly requested.
2. Do not touch `main`, archive branches, marketplace installs or any release/tag outside the narrowly authorized `v0.0.0-mvp-beta` replacement flow in `NTH-CD-056`.
3. Do not reintroduce archived plugin runtime compatibility.
4. Do not add fake build/test/typecheck scripts before implementation exists.
5. Do not add a new package outside the approved 10 packages without a tracked decision.
6. Do not expose internal multi-agent/team/squad concepts to the user-facing product model.
7. Do not call general OpenAI, Anthropic or other model inference APIs directly from Thoth core/daemon as a substitute for a harness/provider session.
8. Do not let provider-native session handles, permissions or model settings become task authority; Thoth records them as execution evidence and resume metadata only.
9. Do not implement a local zero-shot semantic classifier for route, workspace intent, clarification strategy or loop strategy.
10. Local code may only honor explicit user controls, validate schemas, enforce permissions, gather mechanical evidence and maintain authority state.
11. Do not let Clarify mutate workspace files, install dependencies, commit, push, delete or rewrite workspace content.
12. Do not split Plan and Execute into separate Thoth role sessions in MVP; they are one PlanExec provider session.
13. Do not auto-approve provider permission requests merely because PlanExec provider questions are auto-answered after contract freeze.
14. Do not copy Multica source code into the Thoth repository.
15. Do not stage or commit `.agent-os/upstreams/` raw upstream cache.
16. Do not delete tracked `_paseo/` implementation seed material merely because imports, types or tests are temporarily broken.
17. Do not include voice, audio, speech or dictation upstream material in the current MVP implementation seed.
18. Do not rely on npm install lifecycle scripts for required toolchain setup; use explicit root scripts for native packaging/toolchain work.
19. Do not stage or commit `.dev/`, `.agent-os/artifacts/`, generated Android/iOS native folders or APK artifacts.
20. Do not create a separate mock, reduced, debug-only or agent-facing UI as the primary Thoth I review surface.
21. Do not use manual UI exploration as the ordinary agent validation gate; agents should use repository tests, typechecks, builds and explicit smoke commands.
22. Do not auto-probe, reuse, stop or restart `localhost:6767` / `127.0.0.1:6767` from Thoth runtime. That port is reserved for the user's local Paseo/legacy daemon.
23. Do not place relay pairing tokens in URL query strings, logs, screenshots, docs examples or final reports.
24. Do not stage or commit `packages/desktop/release/`.
25. Do not begin production implementation while the target module boundary, authority owner or canonical interface is unresolved.
26. Do not replace a final module with a compressed substitute that combines or bypasses responsibilities belonging to later modules.
27. Do not add fallback, compatibility, degraded, provider-specific or test-only production paths unless a tracked user decision explicitly requires that path.
28. Do not let fixtures or mocks bypass the same public API, state machine and lifecycle used by the real product.
29. Do not lower acceptance thresholds, alter success semantics or hide failing measurements to make a real implementation appear complete.
30. Do not change locked architecture implicitly through code; record and approve the architectural change first, then implement it as the new single path.
31. Do not create `.thoth/provider-sessions`, set a provider home per Agent/Task/phase, copy provider credentials/config/transcripts/cache, or inspect provider-native persistence from core/daemon business logic.
32. Do not represent a Task phase by a hidden foreground Agent status. Task, PhaseRun, ExecutionAttempt and ProviderThread must retain separate identities and lifecycles.
33. Do not place Runtime Truth fields such as attempt ids, generations, budgets, leases, receipts, hashes or cursors into Agent Harness cognitive context. They may exist only in daemon authority, adapter calls, audit evidence and UI projections.

## Non-Goals

1. Implementing daemon, SQLite authority, drivers, TUI, desktop app, mobile app, relay or CLI.
2. Producing runnable MVP behavior.
3. Porting archived plugin commands.
4. Preserving old 0.4.x changelog as the active product history.
5. Maintaining the archived Python package for compatibility.
6. Publishing npm packages, running EAS cloud builds, app-store submission, production web/Relay deployment or producing a real iOS build on Linux. The narrowly authorized GitHub Actions and Release exception is defined by `NTH-CD-056`.
7. Building a second development-only product surface that behaves differently from the releasable Thoth UI.
8. Deploying production relay `relay.thoth.seeles.ai` or production app `app.thoth.seeles.ai`.
9. Using Code4Agent as the current relay deployment authority after the independent `SeeleAI/Thoth-Relay` decision.
